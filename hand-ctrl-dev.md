# hand-ctrl：端侧隔空手势控制器（键鼠双控）开发需求文档
> 用途：直接保存为 `hand-ctrl-dev.md` 粘贴发给 CodeBuddy，严格按本文档分步迭代开发
> 项目定位：Linux 端侧 C++ 纯离线隔空手势控制器，同时输出键盘+鼠标系统事件；不依赖创意新颖度，依靠**标准化工程架构、Linux 底层字符设备调用、端侧模型部署优化、时序防抖算法**构建应届生差异化竞争力；开发环境限定 PC / WSL Ubuntu，推理层预留 RKNN 抽象接口，后续仅替换子类即可无缝移植 RV1106 瑞芯微开发板。

## 一、手势规则总表（唯一基准，所有阈值外置配置文件）
所有时间阈值、距离阈值、位移比例阈值全部写入 `config/gesture_config.json`，代码中不写硬编码魔法数字。

| 序号 | 手势动作 | 判定规则（默认配置阈值） | 最终输出系统事件 | 生效状态前提 |
| ---- | -------- | ------------------------ | ---------------- | ------------ |
| 1 | 握拳长按锁定/解锁 | 握拳姿态持续保持 ≥1.2s | 锁定模式开关切换 | IDLE 空闲状态直接触发 |
| 2 | 五指张开跟随鼠标 | 所有指尖到掌根欧式距离均超出距离阈值 | 掌心坐标映射鼠标指针相对移动 | 仅锁定 LOCKED 状态生效 |
| 3 | 握拳短按左键 | 握拳时长 0~0.5s：单击；＞0.5s 持续按住：左键拖拽 | 鼠标左键单击 / 拖拽 | 仅锁定 LOCKED 状态生效 |
| 4 | OK 手势 | 拇指与食指指尖闭合形成闭环 | 鼠标右键单击 | 仅锁定 LOCKED 状态生效 |
| 5 | 食指横向滑动 | 手腕坐标基本静止，食指尖横向位移≥图像宽度30% | 左右翻页键盘按键 | 仅锁定 LOCKED 状态生效 |
| 6 | 竖拇指确认 | 仅拇指伸直，其余四指收拢弯曲 | 回车键确认 | 仅锁定 LOCKED 状态生效 |

## 二、完整技术栈
- 编程语言：C++17
- 构建工具：CMake
- 图像采集与处理：OpenCV 4.x，固定采集分辨率 640×480、帧率30fps
- 端侧推理框架：PC 本地 NCNN；预留 RKNN-Toolkit2 抽象适配层
- Linux 底层依赖：`uinput` 用户态输入子系统、`ioctl` 系统调用、字符设备权限管理
- 核心算法：卡尔曼多维时序滤波、有限状态机 FSM、二维向量欧式距离、帧间位移速度计算
- 辅助工具：JSON 配置解析库、Git 版本管理

## 三、项目文件目录结构
```
hand-ctrl/
├── CMakeLists.txt            # 全局编译规则：C++17、OpenCV、NCNN 依赖链接
├── config/
│   └── gesture_config.json   # 所有手势阈值、手势-按键映射统一配置文件
├── models/
│   └── ncnn_model/           # palm_det、hand_landmark 两套NCNN param/bin模型文件
├── src/
│   ├── main.cpp              # 程序入口、多线程创建、全模块调度联动
│   ├── camera_capture/       # OpenCV摄像头采集封装类
│   ├── infer_base/           # 推理抽象基类InferBase + NCNN实现子类 + RKNN空预留框架
│   ├── gesture_fsm/          # 关键点枚举定义、卡尔曼滤波封装、手势状态机核心逻辑
│   ├── linux_uinput/         # uinput封装，虚拟键盘+虚拟鼠标双设备，RAII自动释放销毁
│   └── utils/common.h        # 通用工具函数、结构体、全局常量定义
└── README.md                 # 项目说明、部署教程、性能指标、移植规划
```

## 四、强制开发约束规则（AI必须严格遵守）
1. 串行模块化开发：单个模块编译、运行、异常校验无Bug后，再进入下一模块编写，禁止一次性输出完整全量工程代码；
2. 每次仅输出当前开发阶段对应的代码片段，**所有代码必须添加详尽中文注释**，标注函数作用、参数含义、实现思路、注意事项，便于自学理解；
3. 代码编译报错、运行异常时，先给出自查排查思路与调试方法，确认无法解决后再提供修正后的代码；
4. 若遗忘 C++ 语法知识点，仅给出极简可运行示例，不做无关知识点冗余拓展；
5. 涉及 Linux `uinput` 权限不足、设备挂载、用户组授权问题，直接附带可复制执行的终端命令；
6. 推理层严格遵循面向接口编程思想：上层手势业务逻辑仅依赖抽象基类 `InferBase`，严禁直接耦合 NCNN/RKNN 底层API；
7. 所有状态枚举、坐标索引、错误码、固定常量统一收拢在头文件中，代码杜绝魔法数字；
8. 每个模块强制增加异常捕获与错误打印：摄像头打开失败、模型文件不存在、uinput设备创建失败、内存分配异常等场景均输出清晰中文日志；
9. 内存、文件句柄、Linux设备句柄遵循 RAII 自动释放原则，避免程序崩溃后资源残留。

## 五、七阶段完整开发流程 + 每阶段验收标准
### 阶段1：环境部署 + 项目工程骨架搭建
**工作内容**
1. 提供 Ubuntu22.04 / WSL 双版本 OpenCV、NCNN 环境部署方案（apt 包管理器快速安装 + 源码编译高性能安装两种方式）；
2. 编写完整可直接使用的顶层 `CMakeLists.txt`，开启C++17标准，自动链接OpenCV与NCNN依赖；
3. 一键创建整套目录结构与所有空头文件、源文件；
4. MediaPipe Hands 双模型（手掌检测+手部关键点）完整转换流程：TFLite → ONNX → NCNN，附带每一步执行命令、校验方法。

**验收标准**
执行 `cmake .. && make` 编译无报错，空入口 `main.cpp` 可正常运行，目录层级完整无误。

### 阶段2：摄像头采集模块封装
**工作内容**
1. 基于 OpenCV `VideoCapture` 封装独立 Camera 类，对外提供打开设备、读取单帧图像、释放资源三个核心接口；
2. 增加分辨率、帧率参数配置，读取失败输出明确错误日志。

**验收标准**
单独编译运行可弹出窗口实时显示摄像头画面，断开摄像头设备程序能捕获异常并友好退出。

### 阶段3：推理抽象层 + NCNN 模型推理模块
**工作内容**
1. 定义纯虚基类 `InferBase`，规定统一入参（OpenCV图像）、统一出参结构体（21个手部关键点坐标+置信度）；
2. 编写 `NCNNInfer` 子类完成模型加载、图像预处理、推理执行、关键点后处理解析；
3. 搭建空壳 `RKNNInfer` 子类框架，仅实现基类虚函数声明，不填充具体逻辑，用于后续移植扩展。

**验收标准**
传入本地测试图片可正确推理输出21组手部关键点坐标，RKNN预留子类编译无告警。

### 阶段4：卡尔曼滤波 + 手势有限状态机（项目核心模块）
**工作内容**
1. 封装卡尔曼滤波工具类，对21个关键点二维坐标分别做时序平滑，抑制画面抖动、手部微颤造成的跳变误识别；
2. 定义完整状态枚举：`IDLE`空闲、`LOCK_WAIT`等待锁定、`LOCKED`锁定运行、`TRIGGER`手势触发；
3. 依据第一部分手势规则表，完整实现6种手势判定逻辑，时长、距离、位移全部读取JSON配置参数。

**验收标准**
终端实时打印当前运行状态、识别到的手势名称，1.2s锁定倒计时计时精准无偏差，无频繁误判。

### 阶段5：Linux uinput 虚拟键鼠双设备封装
**工作内容**
1. 封装 uinput 管理类，一次性创建虚拟键盘+虚拟鼠标两个HID设备；
2. 封装按键发送、鼠标移动、左键单击/拖拽、右键单击底层接口；
3. 利用C++析构函数RAII机制，程序正常退出/异常退出自动销毁uinput设备，避免 `/dev/uinput` 卡死残留；
4. 附带设备权限放开、用户组加入的shell命令。

**验收标准**
单独调用接口可全局触发空格键、鼠标移动、左右按键，强制kill进程后 `/dev/uinput` 无残留占用。

### 阶段6：多线程解耦 + 全项目联调优化
**工作内容**
1. 主线程做调度器，拆分图像采集线程、AI推理线程，互相解耦避免阻塞卡顿；
2. 状态机识别有效手势后自动映射调用uinput发送系统输入事件；
3. 增加耗时统计日志，打印单帧推理耗时、端到端总延迟，做性能优化。

**验收标准**
整套程序端到端总延迟稳定 ≤50ms，长时间连续运行无崩溃、无大量误触发、画面流畅不卡顿。

### 阶段7：最终收尾优化 + 文档落地
**工作内容**
1. 接入JSON配置文件，实现所有手势阈值、手势绑定按键可视化自定义；
2. 录制演示效果视频，统计性能数据写入README；
3. README 写明 RV1106 移植方案：仅替换RKNN推理子类，上层业务代码零修改；
4. 整理开发踩坑记录、编译依赖问题汇总。

**验收标准**
配置文件修改参数程序实时生效，README文档完整清晰，项目可直接打包上传GitHub。

## 六、本次首轮启动需要输出的内容
1. 项目根目录完整 `CMakeLists.txt` 代码；
2. Ubuntu22.04 / WSL 环境 OpenCV、NCNN 详细部署分步教程；
3. MediaPipe Hands 手掌检测+手部关键点模型 TFLite 转 NCNN 全套执行命令与校验步骤。

## 七、README 强制撰写要点
1. 完整粘贴6条手势映射对照表；
2. 突出项目差异化优势：纯C++全链路实现、完全离线运行、键鼠双设备控制、握拳锁定防误触机制、端到端延迟＜50ms、推理框架可插拔解耦；
3. 说明运行需要`sudo`的原因：`/dev/uinput`字符设备权限限制，附带授权命令；
4. 后续迭代规划：NCNN替换为RKNN、交叉编译移植RV1106边缘开发板。

## 八、最终简历精简描述（直接复制使用）
hand-ctrl：Linux端侧C++隔空手势键鼠控制器。基于OpenCV完成摄像头图像采集，使用NCNN部署MediaPipe Hands轻量化模型解析21维手部关键点；采用卡尔曼滤波平滑坐标抖动，通过有限状态机FSM完成6类手势时序判定；调用Linux uinput用户态输入子系统构建虚拟HID键鼠设备，全局注入键盘鼠标系统事件，整体端到端运行延迟控制在50ms以内。推理层采用抽象基类架构解耦NCNN/RKNN框架，仅替换推理子类即可零改动交叉编译移植至RV1106嵌入式开发板；设计握拳长按锁定机制大幅降低环境干扰误触率，所有判定阈值外置JSON配置文件，工程健壮性与可移植性强。