{
  "version": "2.0.0",
  "_说明": "hand-ctrl v2 单指控制方案配置文件。所有阈值外置，代码禁止硬编码魔法数字。交互模型：锁定后食指指尖绝对定位鼠标，快速下点=单击，下点保持+移动=拖拽；握拳1.2s=锁定/解锁（唯一姿态手势）。",

  "camera": {
    "width": 640,
    "height": 480,
    "fps": 30
  },

  "screen": {
    "screen_width": 1920,
    "screen_height": 1080,
    "_说明": "目标屏幕分辨率（像素）。食指指尖在摄像头画面的位置将等比映射到该屏幕。若实际分辨率不同请修改"
  },

  "lock": {
    "hold_time_ms": 1200,
    "state_cooldown_ms": 1500,
    "_说明": "握拳长按锁定/解锁阈值（毫秒）；state_cooldown_ms 为状态切换防抖冷却时间（毫秒），防止脸部误判导致状态疯狂跳动"
  },

  "kalman": {
    "process_noise": 1.0,
    "measure_noise": 4.0,
    "_说明": "卡尔曼滤波参数：process_noise 越大越信任测量值（响应快但抖），measure_noise 越大越信任预测值（平滑但延迟）"
  },

  "fist": {
    "distance_threshold_px": 160,
    "_说明": "判定握拳：所有指尖到手腕距离均小于此阈值（像素）。双模型下脸部已被 palm 过滤，真手握拳约60-140px，设160放宽"
  },

  "click": {
    "press_speed_px": 60,
    "press_time_ms": 250,
    "release_time_ms": 250,
    "_说明": "食指单击判定：食指尖垂直下落速度超过 press_speed_px（像素/秒）视为下点按下；按下后在 press_time_ms 内回弹（指尖抬起）视为单击。若按住超过 press_time_ms 则视为拖拽开始"
  },

  "drag": {
    "move_time_ms": 400,
    "release_time_ms": 300,
    "_说明": "拖拽判定：下点按住超过 press_time_ms 后进入拖拽（左键保持按住）；松开（指尖抬起）触发拖拽结束。move_time_ms 为进入拖拽的最短按住时长（冗余字段，预留）"
  },

  "key_map": {
    "_说明": "保留字段（v2 已取消手势按键映射，此段仅作兼容占位，可删除）"
  }
}
